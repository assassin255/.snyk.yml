terraform {
  # This repository requires at least Terraform 1.0.0.
  required_version = ">= 1.0.0"
}

# The root module acts as a container for infrastructure configurations
# and CI/CD stack synchronization.
